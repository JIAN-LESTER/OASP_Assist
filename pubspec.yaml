name: capstone_project
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  firebase_analytics: ^12.0.0
  cloud_functions: ^6.0.0
  firebase_storage: ^13.0.0
  google_sign_in: ^6.3.0
  generative_ai_dart: ^0.1.2
  file_picker: ^10.1.9
  provider: ^6.1.5
  flutter_dotenv: ^5.2.1
  path_provider: ^2.1.5
  pdf: ^3.11.3
  
  syncfusion_flutter_pdf: ^29.2.5
  foundation: ^0.0.5
  intl: ^0.20.2
  uuid: ^4.5.1
  http: ^1.4.0
  vector_math: ^2.1.4
  fl_chart: ^0.70.0
  flutter_pdfview: ^1.4.0+1
  # advance_pdf_viewer2:
  #   path:
  # #     # C:/Users/zeldr/Documents/Github/oasp_capstone_project/advance_pdf-viewer2
  #     C:\Users\jianl\OneDrive\Desktop\Java Programming\lib\oasp_capstone_project\advance_pdf-viewer2

  advance_pdf_viewer2: ^2.0.5+1

  logging: ^1.3.0
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  flutter_linkify: ^6.0.0
  docx_to_text: ^1.0.1
  archive: ^3.3.6

  speech_to_text: ^7.2.0

  typed_data: ^1.4.0
  permission_handler: ^12.0.1
  image_picker: ^1.1.2
  google_mlkit_text_recognition: ^0.15.0
  google_speech: ^5.3.0

  shared_preferences: ^2.5.3
  cached_network_image: ^3.4.1
  circle_nav_bar: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - lib/images/google.png
    - lib/images/phone_number.png
    - lib/images/guest.png
    - lib/images/oasp.png
